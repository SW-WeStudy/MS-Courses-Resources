version: '2'

services:
  myapp:
    tty: true
    image: docker.io/bitnami/laravel:7-debian-10
    environment:
      - DB_HOST=westudydb.ce9xg8lh3vrl.us-east-2.rds.amazonaws.com
      - DB_USERNAME=root
      - DB_DATABASE=Luis99.06
      - DB_PASSWORD=westudydb
    ports:
      - 8000:8000
    volumes:
      - ./:/app
    # privileged: true # Privileged mode could be required to run this container under Windows
